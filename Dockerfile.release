FROM complywiseacr.azurecr.io/bis-system-v5:984a5a4

WORKDIR /app

# Copy updated knowledge base, routes, release manifest, and playground
COPY . .

ENV PORT=8001
ENV HOST=0.0.0.0
EXPOSE 8001

CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8001}"]
